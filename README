Slice Renderer by Kevin Jacobs

This is a simple slice rendering program. It can draw single slices, multiple axis-aligned slices, and view-aligned slices. Currently the only source of data the program is compatible with is the CTHead data found at: http://graphics.standford.edu/data/voldata

